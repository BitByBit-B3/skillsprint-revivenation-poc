{
  "name": "skillsprint-revivenation-poc",
  "version": "1.0.0",
  "description": "SkillSprint – Skill Wallet & Micro-Gigs PoC for ReviveNation hackathon",
  "private": true,
  "scripts": {
    "dev:api": "ts-node-dev --transpile-only src/api/index.ts",
    "dev:web": "cd web && npm run dev",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:web\"",
    "build": "tsc && cd web && npm run build",
    "start": "node dist/api/index.js",
    "test:webhook": "node -e \"const crypto = require('crypto'); const secret = 'supersecret'; const payload = JSON.stringify({payoutId:'pay_test',status:'settled',amountLKR:5000,gigId:'g2'}); const signature = crypto.createHmac('sha256', secret).update(payload).digest('hex'); console.log('curl -X POST http://localhost:3001/v1/payouts/webhook -H \\\"Content-Type: application/json\\\" -H \\\"x-paydpi-signature: ' + signature + '\\\" -d \\'' + payload + '\\'');\"",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0",
    "jsonwebtoken": "^9.0.2",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.5",
    "@types/jsonwebtoken": "^9.0.5",
    "typescript": "^5.3.3",
    "ts-node-dev": "^2.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}